import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yahuarcocha.app',
  appName: 'yahaurcocha',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
